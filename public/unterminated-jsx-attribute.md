import { Tab } from '../components/Tab'

# Tabs Example

Use tabs to organize related content.

<Tab label="Installation>
  Run `npm install` to get started.
</Tab>

See the components page for more tab options.
